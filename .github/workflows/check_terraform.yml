name:  Static Terraform Check

on:
  pull_request:
    types: [ opened, synchronize, reopened, edited, labeled, unlabeled ]
    paths:
      - terraform/**
  push:
    branches: [ master ]
    paths:
      - terraform/**
  workflow_dispatch:

concurrency:
  group: terraform-static-check-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write

jobs:
  trivy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Trivy
        uses: aquasecurity/setup-trivy@v0

      - name: Trivy config scan
        id: scan
        working-directory: terraform
        run: |
          set +e
          trivy config . \
            --format sarif \
            --output $GITHUB_WORKSPACE/trivy.sarif \
            --severity HIGH,CRITICAL \
            --exit-code 1
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload Trivy SARIF file
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: ${{ github.workspace }}/trivy.sarif

      - name: Enforce failure on HIGH/CRITICAL
        if: steps.scan.outputs.exit_code != '0'
        run: |
          echo "Trivy found HIGH/CRITICAL issues in ${{ github.workspace }}"
          exit 1
  tflint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v6
        with:
          tflint_version: latest

      - name: TFLint init
        working-directory: terraform
        run: tflint --init

      - name: Run TFLint
        working-directory: terraform
        run: tflint -f sarif > "$GITHUB_WORKSPACE/terraform_tflint.sarif" || true

      - name: Upload TFLint SARIF file
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: ${{ github.workspace }}/terraform_tflint.sarif
